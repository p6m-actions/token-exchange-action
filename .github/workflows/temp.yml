name: Temp Action

on:
  pull_request:

jobs:
  temp:
    name: Create Release
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write

    steps:
      - name: Get P6M YBOR Token
        id: token
        uses: ./
